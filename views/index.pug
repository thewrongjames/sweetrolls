extends layout.pug

block append head
  script(src='/javascript/openRoom.js' defer)

block content
  h1 sweetrolls
  p A web app for rolling dice with friends.
  form#roomForm
    input#roomNameField(type='text', placeholder='Room name')
    p.hidden#noRoomNameWarning Please enter a room name.
    button.openRoomButton(type='submit') Open room
